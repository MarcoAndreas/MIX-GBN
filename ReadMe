# A BGe Score for Tied-Covariance Mixture Gaussian Bayesian Networks

**author:** Marco Grzegorczyk

**affiliation:** University of Groningen, The Netherlands

**email:** m.a.grzegorczyk@rug.nl

This GitHub repository contains R code that accompanies the paper:

```
Grzegorczyk, M. (2026): A BGe score for tied-covariance mixture Gaussian Bayesian networks.
International Journal of Approximate Reasoning.
```

*A link to the published paper will be added once it becomes available.*

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

***Instructions***

1. Save all available R files in a directory (e.g. on your drive **X** in the folder `mGBN`).

2. Open R (or RStudio).

3. Set the working directory to the selected folder.

For example (if you are using Windows), type:

```r
setwd("X:/mGBN")
```

4. The file `WORKED_EXAMPLE.R` provides a step-by-step demonstration of the method.

5. The R commands in `WORKED_EXAMPLE.R` can be executed line by line.
